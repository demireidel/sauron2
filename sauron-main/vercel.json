{
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@10.4.0 --activate && pnpm i --no-frozen-lockfile",
  "buildCommand": "pnpm build",
  "github": { "enabled": true }
}
